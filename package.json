{
  "name": "vocalat-web",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:corpus": "node scripts/evaluate-translation-corpus.mjs",
    "check": "node --check app.js && node --check learning-engine.js && node --check document-analysis.js && node --check morphology.js && node --check ocr.js && node --check service-worker.js && node --check scripts/evaluate-translation-corpus.mjs && npm test && npm run test:corpus"
  }
}
