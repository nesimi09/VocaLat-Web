{
  "name": "vocalat-web",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check app.js && node --check learning-engine.js && node --check document-analysis.js && node --check morphology.js && node --check ocr.js && node --check service-worker.js && npm test"
  }
}
